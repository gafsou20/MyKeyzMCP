# Token MyKeyz longue durée et révocable (généré depuis le CRM).
# Transmis en header `Authorization` brut à l'API (pas de "Bearer").
MYKEYZ_TOKEN=

# Base de l'API MyKeyz (optionnel, valeur par défaut ci-dessous).
MYKEYZ_API_URL=https://api.mykeyz.jg.app

# Environnement multi-tenant à utiliser (optionnel ; sinon celui du token).
# MYKEYZ_ENV=
